#include <iostream>

int main()
{

  // + -> addition
  // - -> subtraction
  // * -> multiplication
  // / -> division
  // % -> modulas

  // ++ -> addition by one
  // -- -> subtraction by one

  // += -> adds to the variable
  // -= -> subtracts from variable
  // *= -> multiples to the varibale
  // /= -> divides from the varibale

  // Note: Make sure when you use += and the rest, you dont write =+ since that wont
  //       do anything in c++ but it wont mark it as compiler error. 




  return 0;
}