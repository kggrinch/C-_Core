#include <iostream>

int main()
{
    // This is the process of code execution

    // 1. Code it written a language like c++, java, or python

    // 2. Code is then compiled into machine code

    // 3. Code is executed
    
    // 4. Code goes into runtime (this is when the code is running)

    // 5. Code is terminated
}